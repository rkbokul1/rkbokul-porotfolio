import React from "react";

const SectionDivider = ({ margin }) => {
    return (
        <div
            className={`w-full max-w-7xl mx-auto bg-white h-[1px] mb-10`}
        ></div>
    );
};

export default SectionDivider;
